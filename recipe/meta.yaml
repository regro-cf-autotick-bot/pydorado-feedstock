{% set name = "pydorado" %}
{% set version = "2.7.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/pydorado-{{ version }}.tar.gz
  sha256: 26ff7c50d794813da2dac28bc56a839f07e050289699f0e64a9367161d6f1ad2

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools
  run:
    - python >={{ python_min }}
    - numpy
    - matplotlib-base
    - scipy
    - future
    - tqdm

test:
  imports:
    - dorado
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/passaH2O/dorado
  summary: dorado - Lagrangian particle routing routine via weighted random walks
  license: MIT
  license_file: LICENSE
  doc_url: https://passah2o.github.io/dorado
  dev_url: https://github.com/passaH2O/dorado

extra:
  recipe-maintainers:
    - elbeejay
    - wrightky
